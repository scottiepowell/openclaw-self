# wger deployment

This directory contains OpenClaw homelab values and notes for deploying wger to Kubernetes.

Upstream Helm chart:
https://github.com/wger-project/helm-charts

Do not vendor the upstream chart here unless we intentionally pin/fork it.
Use Helm repo/chart references plus local values files.
