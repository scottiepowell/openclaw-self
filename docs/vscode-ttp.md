# VS Code TTP

Use VS Code as the main human control surface.

Suggested workflow:
1. Open `~/projects/openclaw-self` with `code .`
2. Ask OpenClaw to use the right repo role.
3. Review plans and diffs in the editor.
4. Run `make doctor` and `make validate` in the integrated terminal.
